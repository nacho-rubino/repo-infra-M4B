docker run -it -v "C:\Users\santi\OneDrive\Desktop\Studies\INFRAESTRUCTURA\infra\repo-infra-M4B:/mnt/host_folder" ubuntu bash
cd /mnt/host_folder
apt update && apt install nano -y
apt-get update && apt-get install -y passwd
apt-get update
apt-get install -y gcc
gcc -o posix posix.c -pthread
./posix
nano menu.sh
chmod +x menu.sh
./menu.sh
sed -i 's/\r$//' menu.sh (esto es para pasarlo a LF de nuevo, si no runnea normalmente, es pq paso a CRLF por el manejo de git, es meter este commando y queda)
docker cp sharp_newton:/menu.sh . (Nombre del container mio era sharp_newton, cambia, esto es para pasar la file a la folder)


Ctrl+O → Save file.

Ctrl+X → Exit.

Ctrl+K → Cut line.

Ctrl+U → Paste line.

CTRL+L -> clear



   